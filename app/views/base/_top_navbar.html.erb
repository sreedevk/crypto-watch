<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
          <div id="nav-toggle" class="inline-block">
            <i class="fa fa-bars fa-2x"></i>
          </div>
          <div class="inline-block header-brand">
            <h3 class="inline-block brand-title">CRYPTOWATCH</h3>
            <span class="inline-block">Cyptocurrency Analysis & Reporting</span>
          </div>
        </div>
        <div class="inline-block">
            <ul class="top-navbar">
              <li>
                <a href="#"><i class="fa fa-area-chart"></i></a>
              </li>
              <li>
                <a href="#"><i class="fa fa-gears"></i></a>
              </li>
              <li class="dropdown">
                <a data-toggle="dropdown" data-target="#notifydrops" href="javascript:void(0);" class="not-btn">
                  <i class="fa fa-bell"></i>
                  <%=session[:notifications].count.to_i%>
                </a> 
               <div href="#notifydrops" class="dropdown-menu notification-card" role="menu" aria-labelledby="dLabel">
                 <div class="box-arrow"></div>
               <%if session[:notifications].present? %>
                <ul class="notification-list">
                  <li class="notfication-header">Notifications</li>
                   <%session[:notifications].each do |noti_item| %>
                   <li class="notification-item">
                     <div class="not-icon">
                       <i class="fa <%=noti_item[:icon_class]%> fa-2x"></i>
                     </div>
                     <div class="not-text">
                       <strong><%=noti_item[:title]%></strong><br>
                       <%=noti_item[:content]%>
                     </div>
                   </li>
                 <% end %>
                  <li class="clear_all">
                    Clear all
                  </li>
                 </ul>
               <% else %>
                <ul class="notification-list no-nots">
                   <li class="no-notification-item">
                     No Notifications Present 
                   </li> 
                 </ul>
               <% end %> 
               </div>
              </li>
              <li class="dropdown">
                <button data-toggle="dropdown" data-target="#user-dropdown" class="btn btn-info dropdown-toggle">
                  <%=current_user.name%>
                </button>
                <div href="user-dropdown" class="dropdown-menu user-menu" role="menu" aria-labelledby="dLabel">
                  <div class="box-arrow"></div>
                  <nav class="user-menu-list">
                    <ul>
                      <li><a href="/users/edit">Settings</a></li>
                      <li><a href="">Privacy</a></li>
                      <li><a href="">Support</a></li>
                      <li><a href="/users/sign_out" data-method="delete">Logout</a></li>
                    </ul>
                  </nav>
                </div>
              </li>
            </ul>
        </div>
    </div>
</nav>
